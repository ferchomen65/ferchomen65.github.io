document.addEventListener("DOMContentLoaded", () => {
    const card = document.querySelector('.card');
    
    // Pequeño retraso para que la animación se aprecie mejor
    setTimeout(() => {
        card.classList.add('visible');
    }, 300);

    console.log("Tarjeta de felicitación cargada correctamente.");
});