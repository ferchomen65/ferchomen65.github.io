/**
 * Lógica de presentación para la tarjeta de Amanda
 */
document.addEventListener("DOMContentLoaded", () => {
    const card = document.getElementById('birthdayCard');

    // Iniciamos la animación de entrada tras un breve delay
    setTimeout(() => {
        card.classList.add('fade-in');
    }, 500);

    // Registro opcional en consola para depuración
    console.log("Programa de felicitación inicializado con éxito.");
});